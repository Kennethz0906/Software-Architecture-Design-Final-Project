spring.application.name=library-system

# MariaDB Configuration (instead of MySQL)
spring.datasource.url=jdbc:mariadb://localhost:3306/library_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=050504092603

# Driver class name for MariaDB
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA Configuration (same as before)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf
spring.thymeleaf.cache=false

# Logging
logging.level.org.springframework=INFO